'''
Created on May 19, 2015

@author: rsong_admin
'''




if __name__ == '__main__':
    fhand=open('./data/DTSC_classification_chembook.csv')
    for line in fhand:
        line=line.rstrip()
        line=line.split(',')
        #strip CAS number:
        line=line[1:]
        if line is '':
            raw_input("find a bad guy")
        for name in line:
            if name is not '':
                print name[:]
        
       
    
    
    class_count=dict()
    
    